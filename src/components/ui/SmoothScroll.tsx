'use client';

export function SmoothScroll({ children }: { children: React.ReactNode }) {
    // Placeholder: lenis or other smooth scroll libraries can be added here later
    return <>{children}</>;
}
